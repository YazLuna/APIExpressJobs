"""This module manages the type of the resource."""
from enum import Enum


class ResourceType(Enum):
    """This class manages the type of the resource."""
    MAIN_RESOURCE = 1
    RESOURCE = 2
